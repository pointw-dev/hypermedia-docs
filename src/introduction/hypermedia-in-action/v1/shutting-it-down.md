# Shutting it down

Explore for a while if you wish, then when you are ready to move on, shut down Event Buddy v1 by running the following in the `server/v1/` folder:

```bash
docker compose down
```
